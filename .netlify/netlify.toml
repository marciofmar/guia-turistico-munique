[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA com hash router não precisa de redirect, mas esta regra
# garante que rotas diretas (ex.: link compartilhado) funcionem:
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200